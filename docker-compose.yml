services:
  pulserealm:
    build:
      context: .
      args:
        VITE_GOOGLE_MAPS_API_KEY: ${VITE_GOOGLE_MAPS_API_KEY}
    ports:
      - "8080:8080"
      - "5063:5063/udp"
    restart: unless-stopped
