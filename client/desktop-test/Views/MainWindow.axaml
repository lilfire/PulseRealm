<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="using:PulseRealm.DesktopTest.ViewModels"
        x:Class="PulseRealm.DesktopTest.Views.MainWindow"
        x:DataType="vm:MainViewModel"
        Title="PulseRealm — Desktop Test Client"
        Width="800" Height="600"
        MinWidth="600" MinHeight="400"
        Background="#0f172a">

  <Window.Styles>
    <Style Selector="TextBlock">
      <Setter Property="Foreground" Value="#e2e8f0" />
      <Setter Property="FontFamily" Value="Segoe UI, Inter, sans-serif" />
    </Style>
    <Style Selector="TextBox">
      <Setter Property="Background" Value="#0f172a" />
      <Setter Property="Foreground" Value="#e2e8f0" />
      <Setter Property="BorderBrush" Value="#475569" />
      <Setter Property="CornerRadius" Value="6" />
      <Setter Property="Padding" Value="6" />
    </Style>
    <Style Selector="Button">
      <Setter Property="Background" Value="#38bdf8" />
      <Setter Property="Foreground" Value="#0f172a" />
      <Setter Property="CornerRadius" Value="6" />
      <Setter Property="Padding" Value="6,4" />
      <Setter Property="FontWeight" Value="SemiBold" />
      <Setter Property="FontSize" Value="13" />
    </Style>
  </Window.Styles>

  <Panel>
    <!-- Main content -->
    <DockPanel>
      <!-- Header -->
      <Border DockPanel.Dock="Top" Background="#1e293b" BorderBrush="#334155" BorderThickness="0,0,0,1" Padding="16,10">
        <WrapPanel>
          <TextBlock Text="PulseRealm" FontSize="18" FontWeight="SemiBold" Foreground="#38bdf8" VerticalAlignment="Center" Margin="0,0,12,0" />
          <Border Background="#854d0e" CornerRadius="9999" Padding="8,2" VerticalAlignment="Center" Margin="0,0,8,0">
            <TextBlock Text="TEST CLIENT" FontSize="11" FontWeight="SemiBold" Foreground="#fde047" />
          </Border>
          <Border CornerRadius="9999" Padding="8,2" VerticalAlignment="Center" Margin="0,0,16,0"
                  Background="{Binding IsConnected, Converter={x:Static vm:MainViewModel.ConnectedBgConverter}}">
            <TextBlock FontSize="11" FontWeight="SemiBold"
                       Text="{Binding IsConnected, Converter={x:Static vm:MainViewModel.ConnectedTextConverter}}"
                       Foreground="{Binding IsConnected, Converter={x:Static vm:MainViewModel.ConnectedFgConverter}}" />
          </Border>

          <TextBlock Text="Server:" FontSize="13" Foreground="#94a3b8" VerticalAlignment="Center" Margin="0,0,4,0" />
          <TextBox Text="{Binding ServerUrl}" Width="200" FontSize="13" VerticalAlignment="Center" Margin="0,0,8,0"
                   IsEnabled="{Binding !IsConnected}" />
          <TextBlock Text="Code:" FontSize="13" Foreground="#94a3b8" VerticalAlignment="Center" Margin="0,0,4,0" />
          <TextBox Text="{Binding JoinCode}" Width="80" FontSize="14" FontFamily="Consolas, monospace"
                   MaxLength="6" VerticalAlignment="Center" Margin="0,0,8,0"
                   IsEnabled="{Binding !IsConnected}" />
          <TextBlock Text="Name:" FontSize="13" Foreground="#94a3b8" VerticalAlignment="Center" Margin="0,0,4,0" />
          <TextBox Text="{Binding PlayerName}" Width="100" FontSize="13" VerticalAlignment="Center" Margin="0,0,8,0"
                   IsEnabled="{Binding !IsConnected}" Watermark="Player name" />
          <TextBlock Text="H(cm):" FontSize="13" Foreground="#94a3b8" VerticalAlignment="Center" Margin="0,0,4,0" />
          <TextBox Text="{Binding HeightCm}" Width="55" FontSize="13" VerticalAlignment="Center" Margin="0,0,8,0"
                   IsEnabled="{Binding !IsConnected}" />
          <TextBlock Text="W(kg):" FontSize="13" Foreground="#94a3b8" VerticalAlignment="Center" Margin="0,0,4,0" />
          <TextBox Text="{Binding WeightKg}" Width="55" FontSize="13" VerticalAlignment="Center" Margin="0,0,8,0"
                   IsEnabled="{Binding !IsConnected}" />
          <Button Content="Join" Command="{Binding JoinSessionCommand}" IsVisible="{Binding !IsConnected}" Margin="0,0,4,0" />
          <Button Content="Leave" Command="{Binding DisconnectCommand}" IsVisible="{Binding IsConnected}"
                  Background="#ef4444" Foreground="White" />
        </WrapPanel>
      </Border>

      <!-- Stats bar -->
      <Border DockPanel.Dock="Top" Background="#1e293b" BorderBrush="#334155" BorderThickness="0,0,0,1" Padding="16,8">
        <StackPanel Orientation="Horizontal" Spacing="32">
          <StackPanel Orientation="Horizontal" Spacing="6">
            <TextBlock Text="Heart Rate" FontSize="13" Foreground="#64748b" VerticalAlignment="Bottom" />
            <TextBlock Text="{Binding HeartRate}" FontSize="22" FontWeight="Bold" Foreground="#f87171" VerticalAlignment="Bottom" />
            <TextBlock Text="bpm" FontSize="12" Foreground="#64748b" VerticalAlignment="Bottom" />
          </StackPanel>
          <StackPanel Orientation="Horizontal" Spacing="6">
            <TextBlock Text="Steps" FontSize="13" Foreground="#64748b" VerticalAlignment="Bottom" />
            <TextBlock Text="{Binding Steps}" FontSize="22" FontWeight="Bold" Foreground="#34d399" VerticalAlignment="Bottom" />
          </StackPanel>
          <StackPanel Orientation="Horizontal" Spacing="6">
            <TextBlock Text="Sent" FontSize="13" Foreground="#64748b" VerticalAlignment="Bottom" />
            <TextBlock Text="{Binding SendCount}" FontSize="22" FontWeight="Bold" Foreground="#a78bfa" VerticalAlignment="Bottom" />
            <TextBlock Text="msgs" FontSize="12" Foreground="#64748b" VerticalAlignment="Bottom" />
          </StackPanel>
          <StackPanel Orientation="Horizontal" Spacing="6">
            <TextBlock Text="Interval" FontSize="13" Foreground="#64748b" VerticalAlignment="Center" />
            <TextBlock Text="{Binding SendIntervalMs, Converter={x:Static vm:MainViewModel.IntervalTextConverter}}"
                       FontSize="16" FontWeight="Bold" Foreground="#fbbf24" VerticalAlignment="Center" />
            <Slider Value="{Binding SendIntervalMs}" Minimum="200" Maximum="5000" Width="100"
                    VerticalAlignment="Center" TickFrequency="100" IsSnapToTickEnabled="True" />
          </StackPanel>
        </StackPanel>
      </Border>

      <!-- Log panel -->
      <Border DockPanel.Dock="Bottom" Background="#0c0f1a" BorderBrush="#334155" BorderThickness="0,1,0,0" Height="120">
        <ListBox ItemsSource="{Binding LogEntries}" x:Name="LogList"
                 Background="Transparent" BorderThickness="0" Padding="8"
                 ScrollViewer.VerticalScrollBarVisibility="Auto">
          <ListBox.ItemTemplate>
            <DataTemplate x:DataType="vm:LogEntry">
              <TextBlock Text="{Binding Text}" FontFamily="Consolas, monospace" FontSize="12"
                         Foreground="{Binding Level, Converter={x:Static vm:MainViewModel.LogColorConverter}}" />
            </DataTemplate>
          </ListBox.ItemTemplate>
        </ListBox>
      </Border>

      <!-- Tracking area -->
      <Border Background="#0f172a" Cursor="Cross"
              x:Name="TrackingArea"
              PointerPressed="TrackingArea_PointerPressed"
              PointerMoved="TrackingArea_PointerMoved">
        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" TextAlignment="Center"
                   Foreground="#475569" IsHitTestVisible="False">
          <Run Text="Click &amp; Move" FontSize="28" FontWeight="Bold" Foreground="#64748b" />
          <LineBreak />
          <LineBreak />
          <Run Text="Mouse clicks → steps (each click = 1 step)" />
          <LineBreak />
          <Run Text="Mouse movement → heart rate (speed maps to bpm)" />
          <LineBreak />
          <Run Text="Join a session above to start sending data." />
        </TextBlock>
      </Border>
    </DockPanel>

    <!-- Search overlay -->
    <Border Background="#0f172a" IsVisible="{Binding IsSearching}"
            HorizontalAlignment="Stretch" VerticalAlignment="Stretch" ZIndex="100">
      <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="12">
        <TextBlock Text="PulseRealm" FontSize="28" Foreground="#38bdf8" FontWeight="Bold" HorizontalAlignment="Center" />
        <TextBlock Text="⟳" FontSize="32" Foreground="#38bdf8" HorizontalAlignment="Center"
                   RenderTransformOrigin="0.5,0.5">
          <TextBlock.RenderTransform>
            <RotateTransform />
          </TextBlock.RenderTransform>
          <TextBlock.Styles>
            <Style Selector="TextBlock">
              <Style.Animations>
                <Animation Duration="0:0:0.7" IterationCount="Infinite">
                  <KeyFrame Cue="0%"><Setter Property="(RotateTransform.Angle)" Value="0" /></KeyFrame>
                  <KeyFrame Cue="100%"><Setter Property="(RotateTransform.Angle)" Value="360" /></KeyFrame>
                </Animation>
              </Style.Animations>
            </Style>
          </TextBlock.Styles>
        </TextBlock>
        <TextBlock Text="{Binding SearchStatus}" FontSize="15" Foreground="#94a3b8" HorizontalAlignment="Center" />
      </StackPanel>
    </Border>

    <!-- Session ended overlay -->
    <Border Background="#e6000000" IsVisible="{Binding SessionIsEnded}" ZIndex="200"
            HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
      <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="16" Width="420">
        <TextBlock Text="Session Complete" FontSize="24" Foreground="#38bdf8" FontWeight="Bold" HorizontalAlignment="Center" />
        <Border Background="#1e293b" CornerRadius="10" Padding="20" BorderBrush="#334155" BorderThickness="1">
          <TextBlock Text="{Binding SummaryText}" FontSize="14" Foreground="#e2e8f0" TextWrapping="Wrap"
                     TextAlignment="Center" LineHeight="24" />
        </Border>
        <Button Content="OK" Command="{Binding DismissSummaryCommand}" HorizontalAlignment="Center"
                Padding="20,6" FontSize="14" />
      </StackPanel>
    </Border>

    <!-- Not found / manual entry overlay -->
    <Border Background="#0f172a" IsVisible="{Binding ShowManualEntry}" ZIndex="100"
            HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
      <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="12" Width="350">
        <TextBlock Text="PulseRealm" FontSize="28" Foreground="#38bdf8" FontWeight="Bold" HorizontalAlignment="Center" />
        <Border Width="48" Height="48" CornerRadius="24" Background="#28f87171" HorizontalAlignment="Center">
          <TextBlock Text="✕" FontSize="24" FontWeight="Bold" Foreground="#f87171"
                     HorizontalAlignment="Center" VerticalAlignment="Center" />
        </Border>
        <TextBlock Text="No server found on the network" FontSize="15" Foreground="#94a3b8" HorizontalAlignment="Center" />
        <TextBlock Text="Make sure the PulseRealm server is running and reachable." FontSize="12" Foreground="#64748b"
                   HorizontalAlignment="Center" TextWrapping="Wrap" TextAlignment="Center" />

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Spacing="12" Margin="0,8,0,0">
          <Button Content="Retry Search" Command="{Binding RetrySearchCommand}" />
          <Button Content="Enter Manually" Command="{Binding ShowManualCommand}"
                  Background="Transparent" BorderBrush="#475569" BorderThickness="1" Foreground="#94a3b8" />
        </StackPanel>

        <StackPanel Spacing="8" Margin="0,8,0,0">
          <TextBlock Text="Enter server address" FontSize="15" Foreground="#94a3b8" HorizontalAlignment="Center" />
          <TextBox Text="{Binding ManualUrl}" FontFamily="Consolas, monospace" />
          <Button Content="Connect" Command="{Binding ManualConnectCommand}" HorizontalAlignment="Center" />
          <TextBlock Text="{Binding ManualError}" Foreground="#f87171" FontSize="13"
                     HorizontalAlignment="Center" IsVisible="{Binding ManualError, Converter={x:Static StringConverters.IsNotNullOrEmpty}}" />
          <TextBlock Text="Use the local IP of the machine running the server&#x0a;(e.g. http://10.0.5.2:5062)" FontSize="12"
                     Foreground="#64748b" HorizontalAlignment="Center" TextAlignment="Center" />
        </StackPanel>
      </StackPanel>
    </Border>
  </Panel>
</Window>
